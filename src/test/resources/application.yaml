spring:
  datasource:
    url: jdbc:h2:mem:cloud-vendor
    username: sa
    password: password
    driverClassName: org.h2.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

    show-sql: true

    hibernate:
      ddl-auto: create-drop



